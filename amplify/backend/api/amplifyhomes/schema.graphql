type Donationinfo @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  photo_url: String
  username: String
  trackid: Int
  date: AWSDate
  deliverycompany: String
  type: String
  weight: Int
}
 